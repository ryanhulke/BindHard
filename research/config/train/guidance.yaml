run_name: "graphAttn_score_guidance"
epochs: 30
batch_size: 32
num_workers: 0
# pin_memory: False
# persistent_workers: true
# prefetch_factor: 0

message_passing_mode: "attention"

hidden_dim: 128
num_layers: 9
norm: true
num_types: 7
guidance_target: "vina_score"
guidance_label_path: "data/crossdocked_vina_score_labels.jsonl"
guidance_lower_is_better: true
