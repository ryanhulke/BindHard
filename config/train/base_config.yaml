run_name: "paper_reproduction"
epochs: 500
batch_size: 64
num_workers: 4
pin_memory: true
persistent_workers: true
prefetch_factor: 2

message_passing_mode: "mlp" # mlp, attention
resume: false
# resume_ckpt: "checkpoints/last.pt"
